This is the blank Repo for the New project .
	1. Open money-claim/pages/api/[dynamic_route] 
	2. Start Creating filename.js and write api functions in it to create api route
	3. Function should be exported and folder name must be specified path must be localhost:3000/api/foldername/[dynamic_route]
	4. Create a mongodb Schema for User and create a route 
	5. All library imports must be on top and component must be imported at last.
	6. const must be used for variable that are defined and values are not reassigned 
	7. EsLint will be configured tommorw 
	8. API routes must be dynamic 
	9. req.query must be parsed as JSON 
	